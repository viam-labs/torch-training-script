# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
pillow>=9.0.0

# Configuration management
hydra-core>=1.3.0
omegaconf>=2.3.0

# Training dependencies
tqdm>=4.64.0
torchinfo>=1.8.0
tensorboard>=2.10.0

# Evaluation dependencies
pycocotools>=2.0.0
matplotlib>=3.5.0

# ONNX export dependencies
onnx>=1.14.0
onnxruntime>=1.15.0

# Standard library dependencies (usually included, but listed for completeness)
# json, os, logging, pathlib, typing, multiprocessing, gc, random - all stdlib

